<div class="cartodb-popup v2">
  <a href="#close" class="cartodb-popup-close-button close">x</a>
  <div class="cartodb-popup-content-wrapper">
    <div class="cartodb-popup-content">
      <h3>{{title}}</h3>
      <p>{{description}}</p>
      <p><img src="{{thumbnail}}" style="width:auto; height:auto; max-height:100px; max-width:100px;"></p>
      <ul>
        <li><a href="{{url}}" target="_blank">Wikimedia</a></li>        
        <li><a href="https://maps.google.co.jp/maps?ll={{lat}},{{lng}}&q={{lat}},{{lng}}&hl=ja&t=m&z=17" target="_blank">Google Maps</a></li>
{{#monumento}}        <li><a href="{{monumento}}" target="_blank">Monumen.to</a></li>{{/monumento}}
      </ul>
    </div>
  </div>
  <div class="cartodb-popup-tip-container"></div>
</div>

SELECT *, ST_X(the_geom) as lng, ST_Y(the_geom) as lat FROM jizos

DELETE FROM jizo_projects;
INSERT INTO jizo_projects (cartodb_id,the_geom, description, files, fullsize, monumento, thumbnail, title, type, url, wikipedia) SELECT cartodb_id,the_geom, description, files, fullsize, monumento, thumbnail, title, type, url, wikipedia FROM jizo_projects_tmp